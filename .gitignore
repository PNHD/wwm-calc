node_modules/
.claude/
.vite/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# MCP server config holds API tokens — keep local, commit the .example instead
.mcp.json


# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# local-only mirror of wherewindsmath.pages.dev (3rd-party app by Ruricon) — never commit/deploy
wwm-math-clone/
live_app.js
live_index.html
